import { Container, Typography } from '@mui/material';

function VideoMerge() {
  return (
    <Container maxWidth="md" sx={{ mt: 6 }}>
      <Typography variant="h4" gutterBottom>
        Merge Videos
      </Typography>
      <Typography>
        This tool will allow you to merge multiple video files into one. (Coming soon)
      </Typography>
    </Container>
  );
}

export default VideoMerge;
